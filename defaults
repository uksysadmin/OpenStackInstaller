# Configuration File
# Amend this to affect defaults for install.sh

# Creates a normal user with following details
DEFAULT_USER=demo
DEFAULT_PASSWORD="openstack"
DEFAULT_TENANCY="default"

# Administrator details
DEFAULT_ADMIN=admin
DEFAULT_ADMIN_PASSWORD="openstack"
DEFAULT_KEYSTONE_ADMIN_TOKEN=999888777666

# Sets up a VLAN network
DEFAULT_PUBLIC_INTERFACE=eth0
DEFAULT_PRIVATE_INTERFACE=eth1
DEFAULT_VLAN_START=100
DEFAULT_NETWORK_SIZE=256
DEFAULT_NUM_NETWORKS=1
DEFAULT_VMNET="10.0.0.0/8"

# All services get this password by default
# e.g. keystone, nova and glance users in MySQL
DEFAULT_MYSQL_PASS="openstack"
LOCAL_MYSQL_INSTALL=1

DEFAULT_VIRT="qemu"
DEFAULT_INSTALL="all"
