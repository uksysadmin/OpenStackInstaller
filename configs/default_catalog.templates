# config for TemplatedCatalog, using camelCase because I don't want to do
# translations for legacy compat
catalog.nova.identity.publicURL = http://%SERVICE_HOST%:$(public_port)s/v2.0
catalog.nova.identity.adminURL = http://%SERVICE_HOST%:$(admin_port)s/v2.0
catalog.nova.identity.internalURL = http://%SERVICE_HOST%:$(public_port)s/v2.0
catalog.nova.identity.name = 'Identity Service'


catalog.nova.compute.publicURL = http://%SERVICE_HOST%:8774/v2/$(tenant_id)s
catalog.nova.compute.adminURL = http://%SERVICE_HOST%:8774/v2/$(tenant_id)s
catalog.nova.compute.internalURL = http://%SERVICE_HOST%:8774/v2/$(tenant_id)s
catalog.nova.compute.name = 'Compute Service'


catalog.nova.volume.publicURL = http://%SERVICE_HOST%:8776/v1/$(tenant_id)s
catalog.nova.volume.adminURL = http://%SERVICE_HOST%:8776/v1/$(tenant_id)s
catalog.nova.volume.internalURL = http://%SERVICE_HOST%:8776/v1/$(tenant_id)s
catalog.nova.volume.name = 'Volume Service'


catalog.nova.ec2.publicURL = http://%SERVICE_HOST%:8773/services/Cloud
catalog.nova.ec2.adminURL = http://%SERVICE_HOST%:8773/services/Admin
catalog.nova.ec2.internalURL = http://%SERVICE_HOST%:8773/services/Cloud
catalog.nova.ec2.name = 'EC2 Service'


catalog.nova.image.publicURL = http://%SERVICE_HOST%:9292/v1
catalog.nova.image.adminURL = http://%SERVICE_HOST%:9292/v1
catalog.nova.image.internalURL = http://%SERVICE_HOST%:9292/v1
catalog.nova.image.name = 'Image Service'
